import * as React from 'react';
import Navigation from './navigation';
import {
	StyleSheet,
} from 'react-native';

const App = () => {
	return (
		<Navigation>
		</Navigation>
	);
};

const styles = StyleSheet.create({});

export default App;
